# Core — LLM inference
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.20.0      # required by transformers for device mapping
sentencepiece>=0.1.99   # required by Qwen2.5 tokenizer
protobuf>=3.20.0        # required by tokenizer serialisation

# Optional — 4-bit quantisation (not used in paper experiments, FP16 only)
# bitsandbytes>=0.41.0

# Data and evaluation
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
